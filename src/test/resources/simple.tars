module test
{
    const i = 1;
    const s = "sss";

    enum TE {
        E1,
        E3,
        E2
    };

    struct SimpleStruct {
        0 require long id=0;
        1 require unsigned int count=0;
        2 require short page=0;
    };

	interface Hello
	{
	    long hello(int no, vector<string> name, map<string, string> tags, out map<int, SimpleStruct> result);
	};
}
