module test
{
    interface Hello
    {
        /**
         * doc string for hello
         * @param no the number
         * @deprecated
         */
        long hello(int no, vector<string> name, map<string, string> tags);
    };
};